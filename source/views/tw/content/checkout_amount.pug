<!DOCTYPE html>
html(lang="en")
  head(itemscope='itemscope', itemtype='http://schema.org/WebSite')
    //共用設定
    include ../layouts/_head.pug
    //獨立CSS
  link(rel="stylesheet", href="../../../assets/css/checkout_amount.css")
  body.checkout_amount(data-page="checkout_amount")
    .main-wrapper.scrollbar-macosx(data-fake-body="")
      .all-wrapper
        // 導覽列
        include ../layouts/_navbar.pug
        // 購物車
        include ../layouts/_shopping-cart.pug
        // 固定左下功能
        include ../layouts/_fixed-left.pug

        // 主要內容
        main
          .header
            .unit-name(data-animate="fadeInUp" data-duration="0.4s" data-delay="0.4s") subtitle
            h1.unit-alias(data-animate="fadeInUp" data-duration="0.4s" data-delay="0.4s") Self-checkout
            p.section-description.innertext(data-animate="fadeInUp" data-duration="0.4s" data-delay="0.4s") Dear customer, welcome to use online Self-checkout service. Please enter the checkout amount and leave your contact information, we will have customer service staff to contact you further, thank you!

          .container
            .content(style="background-image: url('../../../uploads/shopping/checkout-form-success.jpg')")
              //- .result-circle.success
              //-   .line1 Success !
              //-   .line2 Submit successful !
              //-   .brush-img
              //-     img.b-lazy(src="../../../assets/img/blazy.gif" data-src="../../../assets/img/shopping/result-decor.png", alt="")
              section.form
                .form-row
                  .form-grid
                    label.form-group.required
                      p Name
                      input(type="text")
                .form-row
                  .form-grid
                    label.form-group.required
                      p Amount
                      input.enter-amount(type="number")
              p.description Please confirm the payment amount with the service staff first.
              .btn-group
                a.btn.btn-gold(href="javascript:;") Submit

        // 頁尾
        footer
          .footer-top
            .header Achieve Now
            include ../layouts/_quick-link.pug
          include ../layouts/_footer_bottom.pug

  //引入的script
  include ../layouts/_script.pug